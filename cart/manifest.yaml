apiVersion: v1
kind: ConfigMap
metadata:
  name: cart
data:
  REDIS_HOST: redis
  CATALOGUE_HOST: catalogue
  CATALOGUE_PORT: "8080"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart
  labels:
    project: roboshop
    tier: backend
    component: cart
spec:
  replicas: 1
  selector:
    matchLabels:
      project: roboshop
      tier: backend
      component: cart
  template:
    metadata:
      labels:
        project: roboshop
        tier: backend
        component: cart
    spec:  
      containers:
        - name: cart
          image: joindevops/cart:4.0.0
          envFrom:
            - configMapRef:
                name: cart
                
---
apiVersion: v1
kind: Service
metadata:
  name: cart
  labels:
    project: roboshop
    tier: backend
    component: cart
spec:
  type: NodePort
  selector:
    project: roboshop
    tier: backend
    component: cart
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      #nodePort: 30008
 
  